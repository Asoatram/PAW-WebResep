import React from 'react';
import './Spinner.module.css'; // Import the CSS file for styling

const Spinner = () => {
    return (
        <div className="spinner"></div>
    );
};

export default Spinner;